spring:
  application:
    name: tasty-app-bff
  profiles:
    active: ${PROFILE_ACTIVE:dev}
  main:
    allow-bean-definition-overriding: true

server:
  address: 0.0.0.0
  port: 8081
  servlet:
    contextPath: /tasty-app-bff

health:
  config:
    enabled: false

management:
  endpoint:
    health:
      show-details: always
    web:
      exposure:
        include: health,info
